安装依赖步骤：
1. 确保已安装 Python 3.11（或 3.8+ 版本）。
2. 在项目根目录下，运行以下命令安装所需的依赖库：
   pip install -r requirements.txt

启动项目步骤：
依赖下载完成后 直接运行main.py 即可

1.启动过程中，系统会读取根目录下的“美的.xlsx”进行数据清洗、NLP处理和LDA主题挖掘，启动可能需要数秒到几十秒的时间，请耐心等待直到终端显示“Data loaded successfully.”。
2. 服务启动成功后，直接在浏览器中访问：http://127.0.0.1:8000/
